{
  "name": "opportunityhub-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "OpportunityHub frontend with a same-origin proxy to the Render PHP API",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node scripts/serve.mjs",
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
